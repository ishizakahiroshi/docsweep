"""カンバンボード Web UI 用のルーター群。

旧 dashboard（``server/app.py`` の `/` 等）は受信トレイ型で別ルートとして残し、
本パッケージは v0.1.0 第 2 段階の主役 UI である看板（カンバン）ボードを担当する。

- ``board`` モジュール: 看板ボードのレンダリング・トリアージ JSON
- ``cards`` モジュール: ラベル / 期日 / 本文 / archive の書き込み口

書き込み口は必ず ``docsweep.services.*`` を経由し、Web UI に新しい特権を持たせない
（v0.1.0 §11 不変条件）。
"""
